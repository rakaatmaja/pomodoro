import 'package:flutter/material.dart';

//color timer
const TextStyle kTimer = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
    color: Color(0xff706FB8),
    fontFamily: 'Roboto');
const TextStyle kTitleTimer = TextStyle(
  fontSize: 27,
  fontWeight: FontWeight.w600,
);

const whiteColor = Colors.white;

// Icon Constants
const Icon kPlayClockButton = Icon(Icons.play_arrow_sharp);
const Icon kPauseClockButton = Icon(Icons.pause_sharp);

// Time constants
const int kWorkDuration = 25;
const int kShortBreakDuration = 5;
const int kLongBreakDuration = 30;

// Text constants
const String kWorkLabel = 'Work';
const String kShortBreakLabel = 'Short break';
const String kLongBreakLabel = 'Long break';

//style font
const TextStyle kTextStyle = TextStyle(
  fontSize: 45,
);

const TextStyle kTextStyleTimer = TextStyle(
  fontSize: 50,
  fontWeight: FontWeight.bold,
);

const TextStyle kSubtitle = TextStyle(fontSize: 16, color: Colors.black);
